# ============================================================================
# AXIOM — requirements.txt (Inference-only, post-PDF integration)
# ----------------------------------------------------------------------------
# Para correr el pipeline (NO incluye fine-tuning con Unsloth/TRL/PEFT).
# Optimizado para hardware AMD (ROCm 6.2+) y GPUs MI300X.
#
# Si en el futuro hace falta fine-tuning, mover esos paquetes a un
# `requirements-train.txt` separado e instalar a demanda.
# ============================================================================

# --- Core LLM Stack (Versiones Validadas) ---------------------------------
transformers==4.57.1
tokenizers>=0.22.2,<0.23
huggingface-hub==0.36.2
safetensors>=0.7.0

# --- Orquestación y Agentes (LangGraph) -----------------------------------
langgraph>=0.2.0
langchain-core>=0.3.0
pydantic>=2.10.0
pydantic-settings>=2.4.0

# --- Cliente vLLM y Generación Restringida --------------------------------
openai>=1.40.0
instructor>=1.5.0
outlines>=0.0.46
outlines-core>=0.2.14

# --- Embeddings y Vector Store --------------------------------------------
FlagEmbedding>=1.2.10
chromadb>=0.5.0
sentence-transformers>=3.0.0
numpy>=2.0,<2.3

# --- Parsing y APIs Científicas -------------------------------------------
pymupdf>=1.24.0
pymed>=0.8.9
arxiv>=2.1.0
httpx>=0.27.0
httpx-sse>=0.4.0          # SSE streaming para axiom_api.py
aiohttp>=3.10.0
tenacity>=8.5.0

# --- API HTTP wrapper (axiom_api.py) --------------------------------------
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# --- Reportes (PDF generation desde Markdown en writer.py) ----------------
markdown>=3.6
weasyprint>=62.0

# --- Utilidades -----------------------------------------------------------
python-dotenv>=1.0.1
structlog>=24.4.0

# ============================================================================
# Dependencias de SISTEMA requeridas por WeasyPrint (instalar via apt en el
# droplet, NO con pip):
#
#   sudo apt-get update && sudo apt-get install -y \
#   libpango-1.0-0 \
#   libpangocairo-1.0-0 \
#   libcairo2 \
#   libgdk-pixbuf2.0-0 \
#   shared-mime-info

#   apt-get update && apt-get install -y \
#   libpango-1.0-0 \
#   libpangocairo-1.0-0 \
#   libcairo2 \
#   libgdk-pixbuf2.0-0 \
#   shared-mime-info
#
# Sin estas libs, weasyprint importa pero falla al renderizar.
# ============================================================================